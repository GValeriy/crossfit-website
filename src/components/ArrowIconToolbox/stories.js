import React from 'react';

import { storiesOf } from '@storybook/react';

import ArrowIconToolbox from './index';

storiesOf('ArrowIconToolbox', module)
    .add('default', () => (
        <ArrowIconToolbox />
    ));
